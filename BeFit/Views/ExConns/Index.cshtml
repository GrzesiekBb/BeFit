@model IEnumerable<BeFit.Models.ExConn>

@{
    ViewData["Title"] = "Wykonane ćwiczenia";
}

<h1>Wykonane ćwiczenia</h1>

<p>
    <a asp-action="Create">Dodaj nowe wykonanie</a>
</p>

<table border="1" cellpadding="5" cellspacing="0">
    <thead>
        <tr>
            <th>ID</th>
            <th>Ćwiczenie</th>
            <th>Data startu</th>
            <th>Data końca</th>
            <th>Akcje</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ConnId</td>
                <td>@item.ExType?.Name</td>
                <td>@item.SessionInfo?.Start.ToString("dd.MM.yyyy HH:mm")</td>
                <td>@item.SessionInfo?.End.ToString("dd.MM.yyyy HH:mm")</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ConnId">Edytuj</a> |
                    <a asp-action="Details" asp-route-id="@item.ConnId">Szczegóły</a> |
                    <a asp-action="Delete" asp-route-id="@item.ConnId">Usuń</a>
                </td>
            </tr>
        }
    </tbody>
</table>
